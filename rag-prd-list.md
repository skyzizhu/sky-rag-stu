# 标准 RAG PRD 需求 List 与产品经理输出深度指南

> 核心原则：
> **产品经理不需要说明代码怎么写，但必须把产品最终应该怎么表现写清楚。**
>
> 研发看完 PRD 后，可以问：
>
> * 技术上怎么实现？
> * 工作量多大？
> * 选什么技术方案？
>
> 但不应该再问：
>
> * 没搜到怎么办？
> * 到底召回几条？
> * 用户删除知识以后旧答案还能不能返回？
> * 没证据能不能回答？
> * Rerank（重排序）挂了以后整个问答是不是失败？
> * 什么情况算成功？
>
> 后一类问题都应该由 PRD 提前定义。

---

# 一、标准 RAG PRD 总需求 List

一份较完整的标准 RAG PRD，建议至少包含以下内容。

## A. 产品基础

1. 项目背景
2. 产品目标
3. 用户与使用场景
4. 产品范围 / 非范围
5. 产品整体流程

## B. 知识进入系统

6. 知识来源
7. 文件 / 内容校验
8. 文档解析
9. OCR（光学字符识别）/ 特殊内容处理
10. 重复内容与增量入库
11. Metadata（元数据）
12. 内容清洗
13. Chunk（切块）
14. Embedding（向量化）
15. Index（索引）/ 入库完成状态
16. Knowledge Lifecycle（知识生命周期）
17. Version（版本）管理

## C. 用户提问与检索

18. 用户问题输入
19. Session（会话）/ 多轮上下文
20. Query Understanding（问题理解）
21. Query Rewrite（问题改写）
22. Filter（过滤条件）/ Scope（检索范围）
23. Retrieval（检索）
24. Hybrid Retrieval（混合检索）
25. Fusion（结果融合）
26. Rerank（重排序）
27. Retrieval Dedup（检索结果去重）
28. Diversity Control（结果多样性控制）
29. Neighbor Chunk（相邻切块补全）

## D. Context（上下文）与生成

30. Context Selection（上下文筛选）
31. Context Budget（上下文预算）
32. Evidence Gate（证据门控）
33. Generation（答案生成）
34. Citation（引用）
35. Refusal（拒答）/ 不知道
36. 最终答案展示

## E. 数据一致性与产品治理

37. Answer Cache（答案缓存）
38. Cache Invalidation（缓存失效）
39. Knowledge Delete / Archive / Restore（知识删除 / 归档 / 恢复）
40. 权限与 ACL（访问控制列表）
41. Local / Cloud Privacy（本地 / 云端隐私）
42. 安全与敏感内容

## F. 系统稳定性

43. LLM Failure（大模型请求失败）
44. Retrieval Failure（检索失败）
45. Rerank Failure（重排序失败）
46. Parser / Embedding / Index Failure（解析 / 向量化 / 索引失败）
47. Timeout / Retry / Fallback（超时 / 重试 / 兜底）
48. Partial Success（部分成功）

## G. 产品管理能力

49. Knowledge Management（知识管理）
50. System Status（系统状态）
51. Debug / Trace（调试 / 链路追踪）
52. 用户反馈
53. 产品指标

## H. AI 质量

54. Eval Dataset（评测数据集）
55. Retrieval Eval（检索评测）
56. Context Eval（上下文评测）
57. Generation Eval（生成评测）
58. Citation Eval（引用评测）
59. Refusal Eval（拒答评测）
60. Regression（回归评测）
61. E2E Acceptance（端到端验收）
62. GO / NO-GO Gate（上线 / 阻断发布门槛）

## I. 可选增强模块

63. Agentic RAG（智能体式 RAG）
64. GraphRAG（知识图谱增强 RAG）
65. Multimodal RAG（多模态 RAG）
66. Structured Data RAG（结构化数据 RAG）
67. 多知识库
68. Cross-document Summary（跨文档总结）
69. 优化建议

---

# 二、产品基础

# 1. 项目背景

这一部分回答：

> 为什么要做这个 RAG？

需要写清楚：

* 用户当前怎么找资料；
* 当前存在什么问题；
* 为什么普通搜索 / ChatGPT 不够；
* RAG 要解决什么问题。

例如：

> 用户长期积累 PDF、会议纪要、产品文档和研究资料，目前依赖文件夹和关键词搜索查找信息，检索效率低，且无法快速综合多个文档回答问题。
>
> 产品希望让用户将个人资料形成可检索知识库，并通过自然语言进行问答，同时保证答案能够追溯到原始资料。

不要写：

> 因为现在 RAG 很火，所以做一个 RAG。

---

# 2. 产品目标

要写：

> 产品最终希望用户获得什么能力。

例如：

> 用户上传自己的知识资料后，可以直接通过自然语言提问。
>
> 系统需要优先基于知识库已有内容回答问题。
>
> 对关键事实提供来源引用。
>
> 知识库没有足够信息时明确告诉用户无法确认，而不是使用模型自身知识补充答案。

最好同时写“不追求什么”。

例如：

> V1 不做互联网实时搜索。
>
> V1 不替代企业文档管理系统。
>
> V1 不支持多人协作编辑。

---

# 3. 用户与使用场景

不要只写：

> 用户是知识工作者。

要继续写典型场景。

例如：

### 场景 1：查询事实

> “这个项目当时为什么选择方案 B？”

### 场景 2：查找历史资料

> “之前哪份文档提到过退款政策？”

### 场景 3：总结

> “帮我总结过去三个月产品调研文档里的主要结论。”

### 场景 4：比较

> “对比这几份竞品报告中不同产品的定价。”

### 场景 5：列举

> “把所有提到 Agent Memory（智能体记忆）的方案列出来。”

不同场景会影响后面的 Retrieval（检索）、Context（上下文）和 Eval（评测）。

---

# 4. 产品范围 / 非范围

需要明确当前版本到底支持什么。

例如：

### V1 支持

* 文件知识入库；
* 网页知识入库；
* 自然语言问答；
* 多轮对话；
* Citation（引用）；
* Archive（归档）；
* Version（版本）；
* Hybrid Retrieval（混合检索）。

### V1 不支持

* 实时互联网搜索；
* 图片理解；
* Excel 数据计算；
* 企业复杂 ACL（访问控制）；
* Agent（智能体）自动执行操作。

这样可以防止需求无限扩张。

---

# 5. 产品整体流程

PRD 必须有一张完整流程图。

至少包含两条主链路。

## 入库链路

```text
知识来源
→ 校验
→ 解析
→ Metadata（元数据）
→ 清洗
→ Chunk（切块）
→ Embedding（向量化）
→ Index（索引）
→ Knowledge Active（知识生效）
```

## 问答链路

```text
用户提问
→ Query Understanding（问题理解）
→ Retrieval（检索）
→ Rerank（重排序）
→ Context（上下文）
→ Evidence Gate（证据门控）
→ Generation（答案生成）
→ Citation（引用）
→ Answer（答案）
```

产品经理需要定义“有哪些节点”。

具体这些节点怎么写代码，由研发决定。

---

# 三、知识入库链路

# 6. 知识来源

需要写清楚：

> 用户到底可以把什么东西放进知识库。

例如：

> V1 支持：
>
> * PDF
> * DOCX
> * Markdown
> * TXT
> * HTML
> * RTF
> * 网页 URL

同时需要写：

* 单次上传几个；
* 是否支持批量；
* 是否支持文件夹；
* 单文件大小限制；
* URL 是否只抓当前页面；
* 是否允许用户手动输入文本。

---

# 7. 文件 / 内容校验

需要写：

> 什么情况下允许进入后续解析。

例如：

> 文件格式不支持时直接提示“不支持该格式”，不得进入入库流程。
>
> 文件为空时提示“文件无有效内容”。
>
> 单文件超过大小上限时阻止上传。
>
> 一批文件中某一个文件失败，不影响其他文件继续处理。

产品经理应该定义体验。

不需要写 MIME（文件类型识别）检测代码。

---

# 8. 文档解析

需要说明：

> 系统需要从文件中得到什么。

例如：

> 文档解析后至少应获得：
>
> * 正文；
> * 标题；
> * 文档来源；
> * 页码或位置；
> * 可用于 Citation（引用）的定位信息。

还要定义：

> 解析不到正文时不能显示“入库成功”。

---

# 9. OCR（光学字符识别）/ 特殊文档

需要明确：

* 扫描 PDF 支不支持；
* 图片中的文字支不支持；
* 表格支不支持；
* 加密 PDF 怎么办。

例如：

> V1 不支持纯扫描 PDF OCR。
>
> 如果检测到 PDF 无可提取文本，系统提示“当前版本暂不支持扫描 PDF”，不得生成空知识。

或者：

> V1 支持 OCR，OCR 失败时该文件入库失败。

---

# 10. 重复内容与增量入库

这是必须定义的。

例如：

> 同一个文件再次上传，如果内容没有变化，不重复进行 Embedding（向量化），也不创建新版本。
>
> 如果文件内容发生变化，则进入新版本入库流程。
>
> 用户主动点击“重新入库”可以重新执行处理，但内容没有变化时版本号不自动增加。

需要回答：

* 同名但内容不同怎么算；
* 文件改名但内容一样怎么算；
* 内容没变化是否重复计算。

不用规定 MD5 还是 SHA。

---

# 11. Metadata（元数据）

要定义：

> 每个知识对象需要带哪些产品字段。

例如：

* document_id（文档 ID）
* title（标题）
* source（来源）
* category（分类）
* tags（标签）
* author（作者）
* created_at（创建时间）
* updated_at（更新时间）
* version（版本）
* status（状态）

还要说明：

> Metadata 来源冲突时谁优先。

例如：

> 用户手工修改
> ＞ 文档内部 Metadata
> ＞ 文件目录推断
> ＞ 系统默认值

还要定义：

> 哪些 Metadata 可以参与 Filter（过滤）。

---

# 12. 内容清洗

产品经理不需要定义清洗代码，但要说明什么算无用信息。

例如：

> 系统应尽量去除重复页眉、页脚、导航菜单、广告、无意义空行。
>
> 不允许因为清洗把正文标题、表格说明和关键上下文一起删除。

---

# 13. Chunk（切块）

这是 RAG 产品经理必须定义的重点。

例如：

> 默认 Chunk Size（切块大小）= 600。
>
> Chunk Overlap（切块重叠）= 100。
>
> 优先按标题、段落、自然语义边界切分。
>
> 无法按自然边界切分时再使用固定长度。
>
> 每个 Chunk 必须保留：
>
> * 来源文档；
> * 所属位置；
> * Chunk 顺序；
> * Citation（引用）所需信息。

产品经理定义行为和默认值。

不需要写用哪个 Text Splitter（文本切分器）。

---

# 14. Embedding（向量化）

产品经理需要定义：

> 什么情况下内容允许进入 Embedding。

例如：

> 只有解析成功、正文有效并完成 Chunk（切块）的内容才进入 Embedding（向量化）。
>
> 空 Chunk 不进行 Embedding。
>
> Embedding 失败时不得显示整个文档“已完成入库”。

不需要定义 Embedding SDK。

---

# 15. Index（索引）/ 入库完成状态

需要定义：

> 什么叫“入库成功”。

例如：

> 文档只有在解析、Chunk、Embedding、Index（索引）全部完成后，状态才从 Processing（处理中）变为 Active（生效）。

建议状态至少有：

```text
Pending（等待处理）
Processing（处理中）
Active（生效）
Failed（失败）
Archived（已归档）
Expired（历史版本）
Removed（已移除）
```

---

# 16. Knowledge Lifecycle（知识生命周期）

必须定义知识从进入系统到退出系统的完整生命周期。

例如：

```text
New（新建）
↓
Processing（处理中）
↓
Active（生效）
│
├─ New Version（新版本）
│    ↓
│  New Active（新版本生效）
│    ↓
│ Previous → Expired（旧版本转历史版本）
│
├─ Archive（归档）
│    ↓
│ Archived（已归档）
│    ↓
│ Restore（恢复）
│
└─ Delete（删除）
     ↓
   Removed（已移除）
```

---

# 17. Version（版本）管理

需要明确：

> 新版本失败以后旧版本怎么办。

正确的产品规则可以是：

> 新版本完整写入成功以前，旧 Active（生效）版本继续提供检索。
>
> 新版本全部成功以后：
>
> * 新版本 → Active
> * 旧版本 → Expired（历史版本）

不能出现：

> 新版本处理中途失败，旧版本已经失效，用户突然没有知识可用。

---

# 四、用户问题与检索

# 18. 用户问题输入

需要定义：

* 输入框限制；
* 空问题；
* 超长问题；
* 是否允许附件；
* 是否允许选择知识范围；
* 用户是否可以关闭 RAG。

例如：

> 空问题不可提交。
>
> 默认搜索所有 Active Knowledge（生效知识）。
>
> 用户可以手动选择某个文件、某个分类或某个知识库作为检索范围。

---

# 19. Session（会话）/ 多轮上下文

需要明确：

> 多轮问答到底继承什么。

例如：

用户第一轮：

> “Notion AI 多少钱？”

第二轮：

> “它企业版呢？”

系统应该知道“它”指 Notion AI。

因此需要定义：

> 默认使用最近 3 轮 / 6 条消息帮助理解当前问题。

同时要定义：

> 历史对话不能无限进入 Prompt（模型输入）。

---

# 20. Query Understanding（问题理解）

如果项目有这个能力，需要定义：

系统可以从用户问题里理解：

* 查询意图；
* 主题；
* 时间；
* 文档；
* 分类；
* 查询类型。

例如：

> “帮我找 2025 年产品规划里关于 Agent 的内容。”

可以推断：

```text
topic（主题） = Agent
time（时间） = 2025
document_type（文档类型） = 产品规划
```

但是系统不能过度猜测。

---

# 21. Query Rewrite（问题改写）

需要明确：

> 什么情况下允许改写用户问题。

例如：

> Query Rewrite 的目标是提升 Retrieval（检索），不改变用户原始意图。
>
> 改写后的 Query（查询）只用于检索，不替换 UI 中用户原问题。

还要定义失败：

> Rewrite 失败时直接使用原问题继续。

---

# 22. Filter（过滤条件）/ Scope（检索范围）

非常重要。

需要明确三类过滤条件的优先级：

### 用户手动 Filter

最高优先级。

不能自动修改。

### 系统明确规则

例如默认只检索 Active（生效）知识。

### 模型推断 Filter

最低优先级。

例如模型猜用户可能指某个时间段。

如果模型推断 Filter 导致 0 Result（零结果）：

> 可以自动移除推断 Filter，再检索 1 次。

但：

> 用户手动 Filter 永远不能偷偷放宽。

---

# 23. Retrieval（检索）

需要定义：

> 一次检索默认召回多少 Candidate（候选结果）。

例如：

> 默认 Recall Top 10（召回前 10 条）。

还需要明确：

* 只做向量还是多路检索；
* 一路失败怎么办；
* 检索结果为空怎么办。

---

# 24. Hybrid Retrieval（混合检索）

如果使用 Vector（向量检索）+ BM25（关键词检索），需要直接写。

例如：

> 系统同时执行：
>
> * Semantic Vector Search（语义向量检索）
> * Keyword BM25 Search（关键词检索）
>
> 两条链路分别召回候选。

如果一路失败：

> 使用另外一路继续，不直接让整个问答失败。

---

# 25. Fusion（结果融合）

如果产品已经采用 RRF（倒数排名融合），可以直接定义：

> Vector（向量检索）与 BM25（关键词检索）结果使用 RRF 融合。
>
> 默认 RRF constant（融合常数）= 60。

如果没有确定算法，可以只定义：

> 两路结果需要融合后形成统一排序。

---

# 26. Rerank（重排序）

例如：

> Recall Top 10（召回前 10 条）进入 Rerank（重排序）。
>
> Rerank 后默认选择 Top 5（前 5 条）。

失败时：

> Rerank 不可用则直接使用 Fusion（融合）排序继续。

不要因为一个增强模块故障让整个产品不可用。

---

# 27. Retrieval Dedup（检索结果去重）

需要定义：

> 相同或高度重复内容不能占多个 Context（上下文）位。

例如：

> 完全相同的 Chunk（切块）只保留一个。

还可以规定：

> 同一内容来自不同 Version（版本）时默认优先当前 Active（生效）版本。

---

# 28. Diversity Control（结果多样性控制）

需要防止：

> Top 5 全来自同一篇文章。

例如：

> 普通问答中，同一 Document（文档）最多进入最终 Context 3 个 Chunk。

列举型问题可以例外。

---

# 29. Neighbor Chunk（相邻切块补全）

需要说明：

> 如果命中的 Chunk 语义明显被切断，可补充同文档相邻 Chunk。

如果两个相邻 Chunk 本身带有 Overlap（重叠内容）：

> 合并时应去除重复内容。

---

# 五、Context（上下文）与生成

# 30. Context Selection（上下文筛选）

产品经理要明确：

> 检索到的 Candidate（候选结果）不等于最终喂给模型的 Context（上下文）。

例如：

> Recall 10
> → Rerank（重排序）
> → Dedup（去重）
> → Diversity（多样性控制）
> → Neighbor Merge（相邻切块合并）
> → Final Top 5（最终前 5 条）

---

# 31. Context Budget（上下文预算）

必须有总预算。

例如：

> 默认 Context ≤4000 estimated tokens（估算 Token）。

如果超过：

> 按相关性优先级截断，而不是无限塞入。

要定义：

* 文档数量；
* Chunk 数量；
* Token 上限；
* 列举题是否扩容。

---

# 32. Evidence Gate（证据门控）

这是核心红线。

例如：

> Final Context（最终上下文）为空时，不调用 Generation LLM（答案生成模型）。

直接：

> “当前知识库没有找到足够信息。”

如果只有部分证据：

> 可以回答已有部分，但必须明确哪些部分无法确认。

---

# 33. Generation（答案生成）

要定义：

* 回答风格；
* 是否必须基于 Context；
* 可不可以用模型自己的知识；
* 输出结构；
* 最大长度；
* 是否 Streaming（流式输出）。

例如：

> 默认只能使用 Context 中的知识回答。
>
> 模型自身知识不得用于补充事实性结论。

---

# 34. Citation（引用）

要明确：

> 哪些内容需要 Citation。

通常：

* 数字；
* 时间；
* 产品规格；
* 结论；
* 重要事实。

Citation 必须：

> 真正支持前面的内容。

不能只是随便挂一个来源。

---

# 35. Refusal（拒答）/ 不知道

必须明确三种状态：

### 有充分证据

正常回答。

### 有部分证据

部分回答 + 明确缺口。

### 无证据

拒答。

例如：

> “当前知识库未找到能够支持该问题的资料。”

禁止：

> 没查到以后用模型常识继续答。

---

# 36. 最终答案展示

需要定义最终 UI（用户界面）至少包含：

* Answer（答案）
* Citation（引用）
* Source（来源）
* 未确认内容
* 可能的检索范围

根据产品需要可以增加：

* 相关文档；
* Confidence（置信度）；
* 查看原文。

---

# 六、缓存与一致性

# 37. Answer Cache（答案缓存）

需要定义：

> 什么情况下可以复用历史答案。

例如：

相同：

* question（问题）
* filter（过滤条件）
* knowledge scope（知识范围）
* session context（会话上下文）
* product mode（产品模式）

才允许命中。

---

# 38. Cache Invalidation（缓存失效）

知识变化以后，旧答案很可能已经过期。

因此至少以下动作后应让相关 Cache（缓存）失效：

* 新知识入库；
* Reingest（重新入库）；
* New Version（新版本）；
* Archive（归档）；
* Restore（恢复）；
* Delete（删除）；
* Metadata（元数据）变化且影响 Retrieval（检索）。

---

# 39. Archive / Restore / Delete（归档 / 恢复 / 删除）

需要明确：

### Archive（归档）

知识保留，但默认不参与 Retrieval。

### Restore（恢复）

重新进入 Active（生效）。

### Delete（删除）

不再允许 Retrieval。

同时：

> 删除以后旧 Answer Cache（答案缓存）也不能继续返回这条知识。

---

# 40. 权限与 ACL（访问控制列表）

企业 RAG 必须重点定义。

例如：

> 用户只能检索自己本来就有权限查看的知识。

关键原则：

> 权限校验必须在 Evidence（证据）进入 Context（上下文）前完成。

不能：

> 先把无权限内容放进 Prompt（模型输入），然后指望模型“不说出来”。

---

# 41. Local / Cloud Privacy（本地 / 云端隐私）

如果支持本地模式：

> Local Mode（本地模式）下 Question（问题）、History（历史对话）、Chunk（切块）、Context（上下文）不发送第三方云服务。

本地模型不可用：

> 不允许自动切 Cloud（云端）。

必须由用户主动切换。

---

# 42. 安全与敏感内容

根据业务需要定义：

* PII（个人身份信息）；
* 财务；
* 医疗；
* 公司机密；
* 受限知识。

至少需要回答：

> 什么内容允许进入模型？
>
> 什么内容允许被 Citation（引用）？
>
> 什么内容用户无权访问？

---

# 七、失败与兜底

# 43. LLM Failure（大模型请求失败）

不能只写：

> 失败重试。

至少要规定：

* 哪些错误重试；
* 重试几次；
* 是否 Backup Model（备用模型）；
* Backup 失败以后怎么办。

例如：

> Generation（答案生成）请求超时最多重试 1 次；仍失败则返回已检索 Evidence（证据）和明确错误，不返回伪造答案。

---

# 44. Retrieval Failure（检索失败）

例如：

> Vector Search（向量检索）失败但 BM25（关键词检索）正常 → BM25 继续。

两路都失败：

> 返回“检索服务暂不可用”。

不能调用 LLM 自由回答。

---

# 45. Rerank Failure（重排序失败）

通常建议：

> 直接退回 Fusion（融合）排序。

这是典型“增强模块失败不阻断主链路”。

---

# 46. Parser / Embedding / Index Failure（解析 / 向量化 / 索引失败）

需要区分：

### 单文件失败

不影响其他文件。

### 整批失败

显示批次失败。

### Embedding 部分失败

文档不能显示完整 Active。

### 新版本失败

旧 Active 保持不变。

---

# 47. Timeout / Retry / Fallback（超时 / 重试 / 兜底）

所有关键外部能力最好都有明确规则。

例如：

> Parser Timeout（解析超时）= X 秒
> Embedding Retry（向量化重试）= X 次
> Retrieval Timeout（检索超时）= X 秒
> Rerank Timeout（重排序超时）= X 秒
> Generation Timeout（生成超时）= X 秒

数值由产品与研发结合实际 SLA（服务水平目标）决定。

但正式 PRD 最终应有确定值。

---

# 48. Partial Success（部分成功）

AI 产品不能只有 Success（成功）/ Failure（失败）。

例如：

> Retrieval 成功，但 Generation（生成）中途断开。

可以定义为：

**Partial Success（部分成功）**

返回：

* 已生成答案；
* Citation（引用）；
* “生成中断”提示。

这样产品状态更符合实际。

---

# 八、管理能力

# 49. Knowledge Management（知识管理）

至少需要：

* 知识列表；
* 状态；
* 来源；
* Version（版本）；
* 更新时间；
* Archive（归档）；
* Restore（恢复）；
* Delete（删除）；
* Reingest（重新入库）。

---

# 50. System Status（系统状态）

可以展示：

* 当前模型状态；
* Retrieval（检索）状态；
* Knowledge（知识）数量；
* Index（索引）状态；
* 本地 / 云端模式。

---

# 51. Debug / Trace（调试 / 链路追踪）

用户不一定需要看到全部。

但内部至少应该能追踪：

> 用户问题
> → Rewrite（改写）
> → Filter（过滤）
> → Recall（召回）
> → Rerank（重排序）
> → Final Context（最终上下文）
> → Generation（生成）
> → Citation（引用）

这样才能知道问题出在哪里。

---

# 52. 用户反馈

建议至少支持：

* 👍
* 👎

必要时增加原因：

* 没找到正确资料；
* 回答不完整；
* 引用错误；
* 回答错误；
* 不应该拒答。

这些反馈未来可以反哺 Eval（评测）。

---

# 53. 产品指标

建议关注：

* 提问次数；
* Active User（活跃用户）；
* Answer Success（回答成功率）；
* Refusal Rate（拒答率）；
* Citation Click（引用点击率）；
* Knowledge Upload Success（知识上传成功率）；
* Average Retrieval Latency（平均检索耗时）；
* Average Generation Latency（平均生成耗时）；
* 用户反馈满意率。

---

# 九、Eval（评测）

# 54. Eval Dataset（评测数据集）

正式 RAG 应该有固定题集。

题目至少覆盖：

* 单文档事实；
* 多文档事实；
* 对比；
* 总结；
* 列举；
* 时间过滤；
* Metadata Filter（元数据过滤）；
* 多轮；
* 无答案；
* 相似内容；
* 冲突内容。

---

# 55. Retrieval Eval（检索评测）

例如：

### Hit@10（前 10 条命中率）

正确 Evidence（证据）有没有进入前 10。

---

# 56. Context Eval（上下文评测）

不能只测 Retrieval。

还应该测：

### Context Hit@5（最终上下文前 5 条命中率）

正确 Evidence 有没有真的进入 LLM。

### Context Precision（上下文精确率）

进入 LLM 的资料有多少是真的有用。

---

# 57. Generation Eval（生成评测）

至少关注：

### Groundedness（有据性）

回答能否被 Context 支撑。

### Completeness（完整度）

用户问的重点有没有覆盖。

### Relevance（相关性）

有没有答非所问。

---

# 58. Citation Eval（引用评测）

至少包括：

### Citation Coverage（引用覆盖率）

该引用的地方有没有引用。

### Citation Correctness（引用正确率）

引用内容是否真的支撑前面的事实。

---

# 59. Refusal Eval（拒答评测）

知识库没有答案时：

> 是否真的拒答。

知识库有答案时：

> 是否错误拒答。

两个方向都要测。

---

# 60. Regression（回归评测）

任何影响 RAG 行为的修改都应该重新 Eval。

例如：

* Chunk（切块）；
* Embedding（向量化）；
* Query Rewrite（问题改写）；
* Filter（过滤）；
* Retrieval（检索）；
* Fusion（融合）；
* Rerank（重排序）；
* Context（上下文）；
* Prompt（提示词）；
* LLM（大模型）。

需要定义：

> 核心指标比 Baseline（基线）下降多少就算 Regression（回归）。

例如：

> 任一核心指标下降 >0.05 → NO-GO（禁止发布）。

---

# 十、整体验收

# 61. E2E Acceptance（端到端验收）

不能只看单节点。

正式上线前应从用户完整路径验收。

例如：

```text
上传
→ 入库
→ Active（生效）
→ 提问
→ Retrieval（检索）
→ Answer（回答）
→ Citation（引用）
→ Archive（归档）
→ 再提问
→ 不应检索到
→ Restore（恢复）
→ 再提问
→ 可以重新检索
```

---

# 62. GO / NO-GO Gate（上线 / 阻断发布门槛）

最终需要明确：

> 什么条件下版本可以上线。

例如：

```text
Retrieval Hit@10 ≥ 0.90
Context Hit@5 ≥ 0.80
Groundedness ≥ 0.90
Citation Correctness ≥ 0.95
Refusal Accuracy（拒答准确率） = 1.00
P0 Issue（P0级问题） = 0
删除知识后旧答案泄漏 = 0
ACL（权限）越权 = 0
```

全部通过：

**GO（允许上线）**

任一阻断项失败：

**NO-GO（禁止上线）**

---

# 十一、可选高级能力

# 63. Agentic RAG（智能体式 RAG）

如果系统允许模型自主多次检索，需要额外定义：

* 最大检索轮数；
* 每轮什么时候继续；
* 什么时候停止；
* 是否允许换 Query；
* 如何防止循环。

---

# 64. GraphRAG（知识图谱增强 RAG）

需要额外定义：

* 什么关系进入 Graph（知识图谱）；
* 什么问题使用 Graph；
* Graph Retrieval（图谱检索）和普通 Retrieval 如何组合；
* Graph 数据更新规则。

---

# 65. Multimodal RAG（多模态 RAG）

需要额外定义：

* 图片；
* 扫描件；
* PPT；
* 图表；
* 视频；
* 音频。

以及：

> Citation（引用）如何定位到图片 / 页面 / 时间段。

---

# 66. Structured Data RAG（结构化数据 RAG）

如果涉及数据库 / SQL：

需要额外定义：

* 用户允许查询哪些数据；
* 能不能写；
* SQL 安全；
* 超大查询；
* PII（个人身份信息）；
* 数据更新时效。

---

# 67. 多知识库

需要定义：

* 用户怎么选择知识库；
* 默认 Scope（检索范围）；
* 是否允许跨库；
* 不同库权限；
* 多库结果排序。

---

# 68. Cross-document Summary（跨文档总结）

如果支持跨文档总结：

要明确：

> 最多涉及多少文档。

> 资料太多时怎么裁剪。

> 是否必须展示所有来源。

---

# 69. 优化建议

PRD 最后可以单独增加：

## 可选优化建议

例如：

| 优先级 | 当前问题                | 建议                               | 预期收益   |
| --- | ------------------- | -------------------------------- | ------ |
| P1  | Citation（引用）只检查是否存在 | 增加 Citation Correctness（引用正确率）评测 | 降低错误引用 |
| P1  | Rerank（重排序）只使用单模型   | 建立 Rerank 对照实验                   | 提升排序质量 |
| P2  | PDF 不支持 OCR         | 增加 OCR（光学字符识别）                   | 扩大知识来源 |

注意：

> **优化建议和当前版本正式需求必须分开。**

不能让研发不知道：

> “这个到底是本期必须做，还是以后可能做？”

---

# 十二、AI 产品经理写 RAG PRD 最简单的检查方法

写完以后，逐条问自己：

### 知识怎么进来？

说清楚了吗？

### 什么知识可以被检索？

说清楚了吗？

### 用户问题怎么被理解？

说清楚了吗？

### 检索不到怎么办？

说清楚了吗？

### Rerank（重排序）挂了怎么办？

说清楚了吗？

### 哪些资料可以进入 Context（上下文）？

说清楚了吗？

### Context 太多怎么办？

说清楚了吗？

### 没 Evidence（证据）能不能回答？

说清楚了吗？

### Citation（引用）到底是什么标准？

说清楚了吗？

### 知识删除以后旧 Cache（缓存）怎么办？

说清楚了吗？

### 新版本失败，旧版本还能不能用？

说清楚了吗？

### Local Mode（本地模式）会不会把资料发 Cloud（云端）？

说清楚了吗？

### 用户没权限的文档什么时候被拦截？

说清楚了吗？

### 什么叫 RAG 效果好？

说清楚了吗？

### 什么条件才能上线？

说清楚了吗？

如果这些问题研发拿到 PRD 后仍然需要回来问产品：

> PRD 还不够完整。

---

# 十三、最后总结：RAG 产品经理真正需要交付什么

可以把标准 RAG PRD 最终归纳为八件事：

## 1. 定义知识

> 什么东西可以进入系统。

## 2. 定义 Knowledge Lifecycle（知识生命周期）

> 怎么新增、更新、归档、恢复、删除。

## 3. 定义 Retrieval（检索）

> 用户的问题如何找到相关知识。

## 4. 定义 Context（上下文）

> 哪些知识最终有资格让 LLM 看到。

## 5. 定义模型边界

> 模型什么时候可以回答，什么时候必须说不知道。

## 6. 定义一致性

> 知识变化以后，旧 Index（索引）、旧 Cache（缓存）、旧答案怎么处理。

## 7. 定义失败路径

> 任何关键节点失败以后，产品应该怎么表现。

## 8. 定义质量

> 什么叫 RAG 做得好，什么条件下可以上线。

最终仍然可以用那句话判断：

> **凡是会影响用户体验、AI 行为、数据正确性、安全、成本或验收结果的事情，产品经理都应该定义。**

而：

> **内部到底用什么框架、数据库、函数、线程或代码方式实现，交给研发。**
