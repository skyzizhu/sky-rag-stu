# RAG 易错问题清单——入库与检索两条流水线的踩坑记录

> 定位：这是我的个人复盘手册。把两条流水线（入库 / 检索问答）里**容易忽略但举足轻重**的问题逐条记录：
> 每条按「容易忽略的点 → 会造成什么状况 → 用户看到的效果 → 怎么解决」展开。
> 标注 ✅ 的条目是本项目（Sky Personal RAG）真实踩过的坑，附案例。
> 配套阅读：[realize.md](realize.md)（每个节点是什么、怎么实现）、[prd.md](prd.md)（完整设计）。

---

# 第一部分：入库流水线（把知识存进去）

入库流水线：`增量判断 → 解析 → 元数据 → 清洗 → 切片 → 向量化 → 索引`。
一句话原则：**入库时犯的错，会在每次问答时重复犯**——检索侧怎么优化都救不回存错的、存漏的、存脏的内容。

---

## 节点 A0：增量判断

### 1. 增量判断不能只对"全库扫描"生效，显式指定文件也要能走 ✅

- **容易忽略的点**：实现时容易写成"只有扫描整个 knowledge/ 目录才比对内容指纹"，而单文件入库（上传、网页入库、管理台重新入库）直接跳过判断全量重跑。表面看"显式指定 = 用户要强刷"，语义上说得通。
- **会造成什么状况**：同一个 URL 重复录入、同一个文件重新上传时，明明内容一个字没变，仍然完整重跑 LLM 打标签、切片、向量化——每次都花一遍钱和时间。
- **用户看到的效果**：录入等十几秒；账单多出来；日志里全是重复劳动。
- **怎么解决**：给入库函数加一个 `skip_unchanged` 参数。显式路径默认保持"强制重入库"语义（管理台的「重新入库」按钮需要它），但网页入库等场景传 `skip_unchanged=True`，让指纹没变的文件直接跳过。
- **本项目案例 ✅**：网页入库最初每次都全量重跑 6.6 秒；修复后同 URL 二次录入 0.4 秒直接跳过。

### 2. 台账（指纹记录）必须在向量写入成功之后落盘 ✅

- **容易忽略的点**：台账更新代码顺手写在了流水线中段——比如在"向量化"之前。正常运行永远发现不了。
- **会造成什么状况**：向量化或写库阶段一旦失败（Ollama 没启动是最常见的），新内容的指纹**已经记进台账**。之后每次入库，增量判断一看"指纹没变"，全部跳过——**这份内容永远进不了知识库**，且没有任何报错。
- **用户看到的效果**：上传成功提示明明显示了，但怎么问都问不到新内容。这是最隐蔽的一类 bug：不报错、难复现、用户以为是自己问法不对。
- **怎么解决**：台账落盘放在所有外部依赖（向量化、写 Qdrant）成功之后；中途任何异常都不落盘，下次入库自动重试。代价只是失败重跑一次，换来"台账与库永远一致"。

### 3. 解析失败的文件不能记台账 ✅

- **容易忽略的点**：台账更新时遍历的是"本次处理的文件列表"，而解析失败的文件也在这个列表里。
- **会造成什么状况**：文件损坏 / 被占用导致解析失败 → 新指纹记进了台账（卡片数 0）→ 之后每次入库都跳过它。更糟的是：**旧版本的向量还留在库里**（它没走"内容更新"的过期流程），检索继续返回过时内容，新内容却永远进不来。
- **用户看到的效果**：明明更新了文档，问到的还是旧说法；且没有任何异常提示。
- **怎么解决**：台账只为"解析成功且切出了卡片"的文件记录。解析失败 = 这次没动它，下次自动重试。

### 4. 台账写盘要原子化，损坏要大声告警 ✅

- **容易忽略的点**：`write_text` 直接覆盖写 JSON，进程崩溃 / 断电 / 并发写会留下**半截文件**；读取时对损坏 JSON 静默返回空。
- **会造成什么状况**：台账损坏 → 全部文件被当成"新文件"→ 走不到"内容更新"的过期逻辑 → 库里旧版本卡片和新版本**同时 active**，检索返回两份矛盾内容。静默降级时你连原因都查不到。
- **用户看到的效果**：同一个问题出现两个不同答案；或者旧答案反复出现。
- **怎么解决**：先写临时文件再 `os.replace` 原子替换；解析失败时打醒目的错误日志并提示重建知识库。台账是增量入库的命根子，它坏要比它丢严重得多——丢了大不了全量重建，坏了会静默污染答案。

---

## 节点 A1：解析

### 5. 表格类文档的字间排版空格必须归一化 ✅（本项目最经典的坑）

- **容易忽略的点**：Word/HTML 表格为了排版美观，栏目名常写成「**工 作 经 历**」「**基 本 情 况**」——字与字之间有空格。解析时觉得这只是格式，原样保留。
- **会造成什么状况**：用户问"工作经历"，jieba 分词和向量匹配去找的是连续的"工作经历"，与卡片里的"工 作 经 历"**永远匹配不上**——向量通道语义对不上（带空格的词向量偏移），关键词通道分词也对不上。
- **用户看到的效果**：内容明明入库了（卡片数正常、管理台能看到），但一问就答"知识库中没有相关内容"。**解析、入库、存储每个环节都"正常"，唯独检索永远失败**——最迷惑的一类问题。
- **怎么解决**：表格文本提取时压缩**汉字之间的空白**（正则：汉字+空白+汉字 → 去掉空白）。英文单词间的空格不受影响。
- **本项目案例 ✅**：应聘面试登记表的 4 段工作经历一直在库里，但"工作经历有几段"永远检索不到。修复 + 重新入库后立即答对。

### 6. Front Matter 解析失败时，声明块要从正文里去掉 ✅

- **容易忽略的点**：Markdown 头部的 YAML 声明（`---` 块）解析失败时，容易直接把**原文全文**（包括声明块）当正文返回。
- **会造成什么状况**：YAML 语法原文（`title: [broken` 这种）被切片、向量化、可被检索——用户提问时命中一段乱码式的元数据文本。同时用户声明的分类/状态全部丢失。
- **用户看到的效果**：检索结果里出现莫名其妙的半截 YAML；声明的分类不生效。
- **怎么解决**：解析失败时返回**去掉声明块之后**的正文，并打警告日志。

### 7. 每种格式都要有"明示的能力边界"

- **容易忽略的点**：docx 只读段落不读表格（表单类文档直接丢一半内容）；PDF 不区分文字版和扫描版；网页抓取不去掉导航/页眉/脚本。
- **会造成什么状况**：扫描版 PDF 解析出空文本或乱码 → 入库后是垃圾卡片，检索到也答不对；网页正文混着导航菜单，污染切片。
- **用户看到的效果**：检索命中了"看起来相关"的卡片，内容却是菜单项或乱码。
- **怎么解决**：解析器对每种格式明确能力边界并在文档中写明（本项目 README 写明"不支持扫描版 PDF"）；解析结果过短时（如正文 < 50 字）明确报错而非静默入库空卡片；扫描版需求 → OCR 兜底（见"未来方向"条目 30）。
- **本项目案例 ✅**：网页抓取会剔除 nav/header/footer/script，正文过短直接报错不入库。

### 8. 外部内容入库前要"消毒"：标题防注入、体积设上限 ✅

- **容易忽略的点**：网页是**不可信输入**。页面 `<title>` 里藏换行符，可以直接把任意键注入 YAML Front Matter（把恶意页面伪装成任意分类的知识卡片）；超大页面可以耗尽内存。
- **会造成什么状况**：元数据投毒（检索时恶意页面冒充其他分类）；或抓取一个几百 MB 的页面导致服务卡死。
- **用户看到的效果**：轻则答案来源标注错乱，重则服务无响应。
- **怎么解决**：标题/URL 写入 Front Matter 前压成单行（换行替换为空格）；抓取用流式读取并设字节上限（如 5MB）。
- **本项目案例 ✅**：两项都已实现。

---

## 节点 A2：元数据

### 9. 目录推断的值要校验枚举，非法值回退默认并告警 ✅

- **容易忽略的点**：从目录名推断领域时（一级目录 = domain），直接拿目录名当值，不校验是否在枚举里。
- **会造成什么状况**：用户建了个 `knowledge/我的笔记/` 目录 → 这批文档 domain="我的笔记" → 界面上按六个领域筛选时，这批文档**永远筛不出来**，像是消失了。
- **用户看到的效果**：文件明明入库成功（总数对得上），按领域筛选却找不到。
- **怎么解决**：推断值不在枚举内时回退默认领域并打告警日志。

### 10. 元数据合并要有明确优先级

- **容易忽略的点**：元数据来自四个来源（系统强制 / Front Matter / 目录推断 / 默认值），如果不定死优先级，同名文件不同入口入库的档案会不一致。
- **会造成什么状况**：手动声明的分类有时生效有时不生效，排查起来像玄学。
- **用户看到的效果**：同一个文件换个入口入库，领域/分类变了。
- **怎么解决**：定死优先级链：**系统强制字段 > Front Matter 声明 > 目录推断 > 默认值**，全项目统一（本项目已在 metadata.py 落实）。

---

## 节点 A3：清洗

### 11. 清洗的目标是"降噪"，不是"削足适履"

- **容易忽略的点**：清洗规则用力过猛——把所有空行压掉、把所有空格压掉、把短行全合并。表格类内容被压成一锅粥。
- **会造成什么状况**：表格行列关系被破坏，切片后每张卡片丢失"这行属于哪个栏目"的上下文。
- **用户看到的效果**：检索命中了碎片内容（如一个电话号码、一个日期），答案引用的卡片完全读不通。
- **怎么解决**：清洗只处理确定的噪音（连续空行、不可见字符、每页重复的页眉页脚），保留结构（Markdown 标题、表格分隔符）；对语义边界（标题、列表）做保护，短行合并要跳过结构性内容。

---

## 节点 A4：切片（最容易踩坑的节点）

### 12. 切块不能按 size 硬截断——必须沿语义边界切 ✅

- **容易忽略的点**：最朴素的实现是每 500 字切一刀。但语义被拦腰斩断：一句话前半在卡片 3、后半在卡片 4。
- **会造成什么状况**：每张卡片单独看都"半句话"；向量化的语义信号被稀释，检索命中了也只有残句可用；LLM 拿到残句只能猜。
- **用户看到的效果**：答案引用的原文读到一半断了；或该命中的内容检索不到（残句的向量与完整问句距离远）。
- **怎么解决**：**按段落/标题切大块 → 大块超限再按句子边界二次切**，永不从词中间断开；标题作为章节上下文注入每张卡片（卡片知道自己在讲哪一章）。
- **本项目案例 ✅**：parser 已按标题层级转 Markdown，chunker 沿标题和段落边界切，不再硬截断。

### 13. 相邻切片必须保留重叠（Overlap），且支持"拼回" ✅

- **容易忽略的点**：切片之间不留重叠，或者留了重叠但检索到相邻卡片时不知道能拼回。
- **会造成什么状况**：关键句正好落在切点上，两张卡片各有一半——两张都召回也只能各答一半。
- **用户看到的效果**：答案"缺最后一口气"；列举类问题漏条目。
- **怎么解决**：相邻卡片保留 10~15% 重叠；检索后做**相邻补全**——同一文档序号连续的卡片自动拼接成一条（拼接时去掉重叠部分），恢复被切断的上下文。
- **本项目案例 ✅**：上下文组装节点有"相邻补全"，登记表的 4 段工作经历正是靠它恢复完整。

### 14. 表格切片要给碎片"带上表头" ✅

- **容易忽略的点**：表格逐行切成卡片后，每行只有裸值（"2025.01-2025.07 | 甫山智能 | 24K"），**没有列头**（起止时间/单位名称/月薪）。
- **会造成什么状况**：卡片内容语义贫瘠，向量检索时这些碎片与任何问题都匹配不上。
- **用户看到的效果**：表格类文档入库后几乎检索不到具体行内容。
- **怎么解决**：表格行切片时把表头（或所在区块的栏目名）注入每张卡片的文本前部；本项目按表格区块切片（一个区块一张卡）天然缓解。
- **本项目案例 ✅**：登记表按区块切片，"工作经历"栏目名出现在卡片文本里——修复空格归一化后即可命中。

---

## 节点 A5：向量化

### 15. 向量化模型是"全库的地基"——换模型 = 全库重建 ✅

- **容易忽略的点**：随手把 embedding 模型从 A 换成 B，以为和换 LLM 一样无缝。
- **会造成什么状况**：库里存的向量是 A 模型算的，新查询用 B 模型算——**两套语义空间的数字没法比较**，检索彻底失效。更隐蔽的是维度恰好相同的情况（都是 4096 维），不报错但结果全是乱的。
- **用户看到的效果**：换了模型后检索结果变得莫名其妙，或直接报维度错误。
- **怎么解决**：入库时校验集合维度与模型维度一致，不一致直接报错并提示重建（本项目已实现）；文档里明确写"更换向量化模型需要重建知识库"。

---

## 节点 A6：索引（Qdrant）

### 16. 重复入库必须"先清旧数据"，且旧版本要过期留痕 ✅

- **容易忽略的点**：重复入库直接 upsert 新卡片。如果新旧卡片的 ID 生成规则里含版本号（本项目 point_id = doc + version + 序号），新旧 **ID 不同、不会互相覆盖**——旧卡片原样留在库里。
- **会造成什么状况**：一个文档入库 3 次，库里有 3 代卡片**同时 active**，检索结果三份重复，Top K 名额被同一内容的三个版本占满。
- **用户看到的效果**：答案里同一段话出现两三遍；有效信息密度骤降。
- **怎么解决**：内容变化的文件重入库前，把旧版本整体标记 expired（留作历史、退出默认检索）或直接删除；**新版写入必须发生在清旧之后**。同时台账要记录真实版本号，否则版本管理形同虚设。
- **本项目案例 ✅**：曾复现同文档 14 张卡片（v1.0 + v1.1 各 7 张）同时 active。

### 17. 文档 ID 必须稳定，删除文件要同步删向量 ✅

- **容易忽略的点**：document_id 随机生成（每次入库都变）或不含路径（不同目录同名文件互相覆盖）；删除磁盘文件时只删文件不删向量。
- **会造成什么状况**：前者：同一文件重入库变成"新文档"，库越来越臃肿；后者：文件删了，检索还能命中"幽灵知识"。
- **用户看到的效果**：问已删除的内容仍然有答案，且来源显示一个已不存在的文件。
- **怎么解决**：document_id 由相对路径哈希生成（同路径永远同 ID，移动/改名 = 新文档，规则要在文档里写明）；入库流程开头扫描台账，"台账有、磁盘无"的文件同步删除其向量并清台账。

---

# 第二部分：检索问答流水线（把答案问出来）

问答流水线：`服务检查 → 提问 → 会话记忆 → Query 理解 → 向量化 → 过滤 → 双路检索 → 融合 → Rerank → 合并去重 → 相邻补全 → 上下文组装 → LLM 生成 → 后处理`。
一句话原则：**LLM 只对"它看到的证据"负责——证据缺了、脏了、串了，答案就错了。检索侧的所有问题最终都表现为"答案不对"。**

---

## 节点 B2：会话记忆 / 多轮对话

### 18. 传给 LLM 的历史里，不能包含当前这个问题 ✅

- **容易忽略的点**：把当前问题 append 进消息列表**之后**，再从列表尾部截取"最近 N 条"作为历史传出去——当前问题被包含在内。
- **会造成什么状况**：LLM 收到当前问题**两遍**（历史一遍 + 正式提问一遍）；且滑窗实际只能覆盖一半的轮次。
- **用户看到的效果**：多轮对话时答案重复啰嗦、或对"上一轮"的理解错位。
- **怎么解决**：构建历史时排除最后一条（`messages[:-1]`），再截取滑窗。
- **本项目案例 ✅**：已修复。

---

## 节点 B3：Query 理解 / 改写

### 19. 计数/列举类问题必须触发"召回扩容"——词表靠不住，要让 LLM 判定 ✅

- **容易忽略的点**："工作经历有几份？"这类问题的答案需要**凑齐全部条目**。默认召回 5 条，可能只包含前 2 段经历的卡片，其余被其他栏目的卡片挤出名额。
- **会造成什么状况**：LLM 看到的证据只有 2 段 → 自信地回答"2 份"。**证据缺了，答案就缺了，而且语气非常肯定**。用户追问后下一轮改写了查询、召回了全部，才自我修正为"4 份"——前后不一致。
- **用户看到的效果**：同一个问题两次回答数字不一样；第一次错得理直气壮。
- **怎么解决**：双通道：① Query 理解让 LLM 输出 `enumeration` 布尔字段做**语义判定**（不限措辞）；② 关键词词表（"几份/几段/有哪些/全部…"）做兜底。判定命中 → 召回翻倍 + 放开单文档上限。三个限制必须一起放开（见下条）。
- **本项目案例 ✅**：第一次答 2 份、追问后答 4 份的真实事故，已用双通道修复。

### 20. 计数问题的三个限制必须同时放开，缺一个就白扩 ✅

- **容易忽略的点**：只把召回数量（Top K）翻倍，忽略了"上下文最多放几条"和"单文档最多几张卡"这两个限制。
- **会造成什么状况**：召回翻倍了，但上下文组装时多出来的卡片被丢弃（条数上限）；或者 4 段经历全在**同一个文档**里，被"单文档最多 3 张"拦住——永远只能数到 3。
- **用户看到的效果**：扩容开关开了，计数答案还是少。
- **怎么解决**：列举模式下三个旋钮一起拧：召回 ×2、上下文条数 ×2、单文档上限放开到不限。

### 21. 过滤条件推断要"宁少勿错" ✅

- **容易忽略的点**：让 LLM 从问题里激进推断过滤条件（领域、分类），一句话推错，正确答案直接被过滤器挡在门外。
- **会造成什么状况**：LLM 看到"周会纪要"猜 domain=work，但用户实际把它存在 reference 目录 → 过滤后该文档永远不出现。
- **用户看到的效果**："知识库中没有相关内容"——其实是被过滤掉的，用户无从知晓。
- **怎么解决**：只允许高置信度的推断（如用户明确说"归档里的"、明确点名文件名）；推断不出就不加过滤；域/分类这类枚举交给界面手动筛选。**宁可范围大一点多检索几条，也不要猜错漏掉正确答案**（本项目在 Query 理解的注释里明确记录了这条实测教训）。

---

## 节点 B6~B8：双路检索与融合

### 22. 只用向量检索是不够的——专有名词、编号、缩写必须配关键词通道 ✅

- **容易忽略的点**：向量检索是"语义相似"，它天然不擅长精确匹配："CHUNK_SIZE 参数写的多少"这类问题，专有名词的向量与问题文本相似度不高。
- **会造成什么状况**：含精确术语/编号/型号的问题，正确文档排在很后面甚至不出现。
- **用户看到的效果**：明显存在的配置名、人名、编号搜不到。
- **怎么解决**：双通道（向量 + BM25 关键词）并行召回，RRF（倒数排名融合）合并两路排名。语义问题向量通道赢，精确匹配问题关键词通道赢，互补。

---

## 节点 B9~B11：Rerank 与合并去重

### 23. Rerank 的分数适合"排序"，不适合当"过滤阈值" ✅

- **容易忽略的点**：让 LLM 给候选打 0~10 的相关度分，然后想"低于 6 分的丢弃"——把主观分当硬门槛。
- **会造成什么状况**：LLM 打分有随机性，同一批候选两次打分不同；主观性强的问题（观点类、对比类）整体分数都偏低或偏高，按固定阈值过滤会时而全删、时而全留。
- **用户看到的效果**：同一问题时而答得出时而"没有相关内容"。
- **怎么解决**：Rerank 分数只用于**排序选 Top K**（取前 N 名），不做绝对阈值过滤。（本项目在 reranker 注释里记录了这条实测教训。）

### 24. 去重要按"内容归一化后"比较，多样性限制要能按需放开 ✅

- **容易忽略的点**：去重时直接比较原文全等；或"单文档最多 3 张"的多样性限制对所有问题一刀切。
- **会造成什么状况**：只差一个空白符的两段内容都被当成不同卡片；列举类问题里同一文档的 4 段经历被多样性限制砍到只剩 3 段——**计数答案永远差一**。
- **用户看到的效果**：计数类答案差一点点（4 段答成 3 段），且每次都恰好差同数量——极难察觉。
- **怎么解决**：去重前做归一化（忽略空白差异）；多样性上限在列举模式下放开（见条目 20）。

---

## 节点 B12~B13：相邻补全与上下文组装

### 25. 相邻拼接之后，必须复核 token 预算 ✅

- **容易忽略的点**：第一轮逐条选取时有 token 上限控制，但"相邻补全"把同文档连续卡片**整块拼接**，拼接块绕开了逐条预算。
- **会造成什么状况**：最终上下文远超 LLM 的窗口限制，LLM 调用直接报错。
- **用户看到的效果**：某些列举类提问必现报错，其他问题正常——非常迷惑。
- **怎么解决**：拼接完成后对总上下文复核 token，超限则截断拼接块（保留编号与出处）。
- **本项目案例 ✅**：已修复（`_truncate_to_tokens`）。

### 26. token 估算要考虑中文——不能按"4 字符 = 1 token"硬算

- **容易忽略的点**：沿用英文经验值（约 4 字符 1 token）估算中文文本。
- **会造成什么状况**：中文实际约 1 字 ≈ 0.6~1 token，估算值比真实值小一半以上 → 上下文实际超限，LLM 报错或被服务端截断。
- **用户看到的效果**：长回答偶发"回答到一半被切断"或 API 报 context length 错误。
- **怎么解决**：中英混合估算：CJK 字符按 ~0.8 token/字，其余按 ~0.3 token/字符（本项目 estimate_tokens 的实现）。

---

## 节点 B14~B15：LLM 生成与后处理

### 27. 流式输出的"思考标签"要跨片段清洗，定稿时全文再清一遍 ✅

- **容易忽略的点**：推理模型（qwen3 等）会输出 `<think>…</think>`，逐片段清洗时标签可能**正好被切在两个流式片段的边界上**，正则匹配不到就原样漏出。
- **会造成什么状况**：用户看到推理过程原文混在答案里；更糟的是带着 think 标签的文本被存进缓存反复复播。
- **用户看到的效果**：答案里夹着一大段英文/中文的"内心独白"。
- **怎么解决**：流式过程中尽力清洗（体验），**答案定稿时对全文再做一次清洗**（正确性），缓存存清洗后的版本。
- **本项目案例 ✅**：已实现 `strip_think_tags` 全文终清。

### 28. 引用编号必须和来源列表严格对位

- **容易忽略的机会点**：prompt 要求 LLM 标注 [1][2]，但上下文里资料的编号与后处理生成的来源列表如果不来自同一份数据，编号就会错位。
- **会造成什么状况**：答案标 [2]，用户点开来源 [2] 却是另一份文档——引用失去公信力。
- **用户看到的效果**：核对来源时发现对不上，从此不再信任引用。
- **怎么解决**：上下文组装时的编号、LLM 看到的编号、后处理的来源列表，三者必须出自**同一个列表对象**（本项目 sources 与 context 编号同源）。

---

## 缓存（横跨两条流水线）

### 29. Q→A 缓存的键必须包含"对话历史" ✅

- **容易忽略的点**：缓存键 = 问题文本 + 检索参数，看似完备——漏了对话历史。
- **会造成什么状况**：多轮会话里"继续""展开讲讲""它多少钱"这类短追问，文本相同但**语义依赖上下文**——命中了别的会话/别的轮次的缓存，返回驴唇不对马嘴的答案。
- **用户看到的效果**：追问"继续"，答案重复上一轮内容或完全无关；不同标签页互相"串答案"。
- **怎么解决**：缓存键加入历史的签名（角色+内容摘要）；仅检索模式（不调 LLM）不读缓存。
- **本项目案例 ✅**：多轮"直接回答数字"的追问曾串出错误答案。

### 30. 失败的、半截的回答绝不能进缓存 ✅

- **容易忽略的点**：用 `finally` 无条件写缓存——LLM 超时抛异常、用户中途点停止，**空答案或半截答案照样被缓存**。
- **会造成什么状况**：一次网络抖动 → 这个问题被永久污染，之后每次问都返回那份坏结果，重试都没有机会。
- **用户看到的效果**："这个问题坏掉了"，怎么问都是空答案或断头话。
- **怎么解决**：只在流**正常完成**且答案非空时写缓存；异常与用户中断（GeneratorExit）不写。
- **本项目案例 ✅**：已修复。

### 31. 删除知识后必须清问答缓存 ✅

- **容易忽略的点**：入库时记得清缓存，删除/清空向量库时忘了。
- **会造成什么状况**：删除某文件后再问同样的问题，**命中缓存返回已删除内容的答案**——像删除操作从未发生过。
- **用户看到的效果**：删了的知识还能"问答出来"，数据一致性崩塌。
- **怎么解决**：删除、移除、清空向量库的所有路径统一清 Q→A 缓存和 BM25 索引缓存（BM25 有数量变化兜底，Q→A 缓存没有，必须显式清）。
- **本项目案例 ✅**：已修复。

---

## 工程层（会话、并发、前端）

### 32. 会话/台账/设置等状态文件必须原子写盘 ✅

- **容易忽略的点**：`write_text` 直接覆盖写。
- **会造成什么状况**：写一半崩溃 → 半截 JSON → 整个会话/设置静默丢失，或应用启动即崩（旧值类型校验缺失时）。
- **用户看到的效果**：会话列表少了几个会话；应用打不开了。
- **怎么解决**：临时文件 + `os.replace` 原子替换；读取时做类型/取值校验，坏值回退默认而非崩溃。
- **本项目案例 ✅**：三处文件全部原子化 + settings 校验回退。

### 33. 流式回答期间前端 DOM 会错位——回答完成后强制重绘一次 ✅

- **容易忽略的点**：Streamlit 等框架流式更新长对话时，前端元素复用可能出现视觉错位（旧答案内容显示到新问题下方）。
- **会造成什么状况**：用户看到新旧内容穿插，以为系统把历史搞乱了（其实数据层顺序完全正确）。
- **用户看到的效果**：对话"看起来"串了，刷新页面又恢复正常。
- **怎么解决**：回答完成并保存后立即 `st.rerun()` 强制从数据完整重绘；数据层顺序正确是重绘能生效的前提。
- **本项目案例 ✅**：已修复，配合滚动锚定重试。

---

# 第三部分：未来方向（已识别、未实施）

### 30+. 图片 / 扫描件的 OCR 解析

- **现状**：扫描版 PDF 与图片文件不支持（文档已明示边界）。
- **规划结论**：RapidOCR（PaddleOCR ONNX 版）做文字底座，Ollama 视觉模型（qwen2.5vl 等）做图表理解增强，`.env` 开关控制；OCR 结果需要缓存避免重复入库反复计算。
- **入库耗时提醒**：扫描件逐页 OCR 约 1~3 秒/页，需要分步进度反馈配合。

### 31+. 记录在案、暂不处理的已知限制

| 限制 | 触发条件 | 为什么暂不处理 |
|---|---|---|
| SSL 证书校验失败自动降级 | 目标网站证书链不完整 | 功能需要（部分站点无法严格校验），入库消息明确提示风险 |
| 设置跨标签页读改写竞争 | 两个标签页同时改设置 | 原子写已防损坏；合并需脏跟踪，单用户场景不划算 |
| 知识目录外同名文件 ID 冲突 | 手动 ingest 目录外文件 | 正常界面操作不可达 |

---

### 32+. 观察清单：切片计数是否从字符切到 Token（2026-09-12 记录）

- **当前状态**：保持字符计数（CHUNK_SIZE=600 字符 + 结构边界），中文为主场景下字符≈token，不换。
- **观察信号**（满足任一即触发改造评估）：① 英文/多语言内容占比明显上升；② 更换 embedding 模型且新模型有严格输入 token 上限；③ 需要按卡片级精确核算 token 成本。
- **改造内容**：打包计数 len(text) → 分词器计数；CHUNK_SIZE=600 字 → 约 512 tokens；全库重建 + 评测基线重跑。
- **结论依据**：中文「字符 ≈ token」近似成立；当前 600 字 ≈ 480~600 tokens，远低于 embedding 模型输入上限，无截断风险。

---

### 33+. 观察清单：PDF 跨页段落被页边界切断（2026-09-12 记录）

- **现象**：PDF 中横跨两页的段落，会在页码标记处被切成两半，且页边界无 Overlap（重叠只在章节内的段落打包时发生）。
- **现有缓解**：① 仅 PDF 受影响（其余 6 类输入无页概念）；② 切片序号全文连续，跨页两半序号相邻，B12 相邻补全可在问答时拼回；③ 断点多落在行/句边界。
- **残留风险**：跨页段落的两半只有一半被召回时，该次回答语义不全。
- **触发信号**（满足即评估改造）：① PDF 内容占比上升且出现"答案引用片段读不通"的反馈；② 需要整段精确引用的场景增多。
- **改造方向**：页码只作元数据标记而不作切分边界（跨页段落合并，页码记「3-4」）；或仅对超过 N 页的大 PDF 启用分页切分。改造会改变页码元数据的精确性，需权衡溯源需求。

## 复盘方法论（写给自己）

1. **"解析、入库、存储都正常，唯独检索不到"** → 先查文本归一化（空格/全半角/大小写），再查过滤条件，最后查缓存。
2. **"同一个问题两次答案不一样"** → 先怀疑证据集合变了（召回不全 / 缓存 / Query 理解改写方向不同），不要先怀疑 LLM。
3. **"偶发报错、无法复现"** → 查外部依赖时序（Ollama/Qdrant 慢）与台账落盘时机。
4. **每修一个 bug，问一句：这是个案还是一类？** ——本清单里 ✅ 的条目，几乎每个都是"一类问题"的代表。
