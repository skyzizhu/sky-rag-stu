# RAG 实现全解 —— 每一个节点的技术、作用、目标与衔接

> 本文基于 Sky Personal RAG 的**真实实现**（V3.0）逐节点展开，写给产品经理。
> 每个节点回答四个问题：用了什么技术 / 这个节点在做什么 / 它的目标是什么 / 它和上下游怎么衔接。
> 搭配界面「调试模式」的 15 节点时间线食用，每个知识点都能在系统里亲眼看到。

---

## 全景：两条流水线

```text
入库流水线（存知识）：文件 → 增量判断 → 解析 → 元数据 → 清洗 → 切片 → 向量化 → 建索引
问答流水线（用知识）：提问 → 服务检查 → Query 理解 → 向量化 → 过滤 → 双路检索
                      → 融合 → 精排 → 组装 → Prompt → LLM → 后处理 → 展示
```

两条流水线在「向量数据库 Qdrant」汇合：入库负责存，问答负责查。

---

# 第一部分：入库流水线（把知识存进去）

## 节点 A0：增量判断（V3.1）

- **技术**：文件内容 MD5 指纹 + 台账文件（index_state.json）。
- **作用**：把每个文件入库时的内容指纹记在台账里；下次入库时对比指纹——没变就跳过。
- **目标**：日常维护知识库只处理"有变化的部分"，省时省钱。
- **衔接**：发生在所有节点之前，决定"哪些文件进入流水线"。文件被删除时，同步清理它的向量数据。
- **注意**：跳过结果必须立即写回台账（实测踩过"清理未落盘、反复触发"的坑）。

## 节点 A1：文档解析（Parser）

- **技术**：pypdf（PDF）、python-docx（Word）、striprtf（RTF）、BeautifulSoup（HTML）、PyYAML（Markdown 头部声明）、内置读取（TXT/MD）。
- **作用**：六种格式长相完全不同，统一读成「正文文本 + 档案卡」。PDF 逐页记录页码标记；Word 的标题样式转成通用标题标记；Markdown 单独拆出文件头声明（Front Matter）。
- **目标**：不管来源是什么，下游看到的都是同一种东西。
- **上游**：knowledge/ 目录里的原始文件；**下游**：元数据生成、清洗。
- **注意**：加密 PDF、扫描版 PDF（纯图片）读不出文字，必须明确报错；解析失败要带文件名提示。

## 节点 A2：元数据生成（Metadata）

- **技术**：路径解析规则 + YAML 解析 + 哈希（document_id）+ 枚举校验。
- **作用**：给每个文档生成 18 个字段的统一档案（来源、路径、领域、分类、主题、标签、版本、状态、各时间戳等）。
- **目标**：让"这条知识是谁、从哪来、属于哪类"永远可查。
- **衔接**：输入来自三层——目录推断（放对文件夹自动分类）、Front Matter 人工声明、系统默认值；输出交给切片和向量库。
- **注意**：优先级是「系统强制字段 > Front Matter > 目录推断 > 默认值」；document_id 按路径生成、永不变化；任何字段不允许缺失。

## 节点 A3：清洗（Cleaner）

- **技术**：正则表达式（不可见字符、空格、空行）；PDF 页眉页脚用"过半页数重复出现"的简单规则识别。
- **作用**：去杂质、保结构——乱码、多余空行、每页重复的页眉页脚清掉；标题层级、段落、页码标记保留。
- **目标**：脏文本会污染检索和生成，清洗是质量的第道防线。
- **衔接**：上游是解析出的原始文本，下游是切片。
- **注意**：清洗前后应可对比查看——排查检索质量问题时这里是第一现场。

## 节点 A4：知识切片（Chunk）

- **技术**：标题/段落优先的自然边界切分 + 固定长度兜底 + 相邻 Overlap；切片编号 = 文档身份证 + 版本号 + 序号（如 doc_a913bc12_v1.1_0003）。
- **作用**：把长文档撕成一张张主题聚焦的"知识卡片"。本项目默认单卡 600 字、相邻重复 100 字。
- **目标**：卡片太长主题混杂检索不准，太短语义断裂答案破碎——切片尺寸是 RAG 最重要的参数。
- **衔接**：上游是清洗后的全文；下游是向量化。每张卡片完整继承文档档案，并补充章节、页码、卡片编号。
- **注意**：编号必须带版本号且稳定——同一文件反复入库不产生重复，内容升级后新旧版本可共存。

## 节点 A5：向量化（Embedding）

- **技术**：qwen3-embedding:4b（2560 维），通过 Ollama 在本机运行，不联网。
- **作用**：把每张卡片的文字变成一串数字（指纹）。意思越相近，指纹越相近——让"语义相似"可以被计算。
- **目标**：为检索做准备：先存指纹，提问时算问题指纹来比对。
- **衔接**：上游是切片，下游是向量库。
- **注意**：文档和提问必须用同一个模型；换模型 = 全部指纹作废，必须重建知识库。

## 节点 A6：索引建立（Qdrant）

- **技术**：Qdrant 向量数据库（HNSW 近似最近邻检索）；同时为 9 个档案字段建立过滤索引。
- **作用**：把「指纹 + 卡片原文 + 完整档案」存进唯一集合 personal_knowledge；支持毫秒级相似检索和按档案字段过滤。
- **目标**：问答时的"双路检索"（向量 + 关键词）都以它为数据源。
- **注意**：关键词通道（BM25）的索引是查询时在内存中现建的（个人知识库规模下毫秒级完成），保证结果与库内数据实时一致；重复入库同一文件 = 旧卡片先转历史/清理，新卡片写入，绝不产生重复。

---

# 第二部分：问答流水线（把答案问出来）

## 节点 B0：服务就绪检查

- **作用**：提问前确认三件事——本机向量化模型在线、向量数据库在线、云端大模型 Key 已配置。
- **目标**：缺什么立刻提示怎么修，不让用户等到一半才报错。

## 节点 B1：提问

- **作用**：接收用户问题，记录时间戳，问答从这里开始。
- **衔接**：问题原文会一路传给 Query 理解、向量化、Prompt 各节点。

## 节点 B2：会话记忆 / 多轮对话【当前直通】

- **作用**：完整 RAG 在多轮对话时会结合历史理解当前问题（"它多少钱"的"它"指谁）。
- **当前状态**：每次问答独立，不带历史——这是后续版本的能力，时间线里如实标注。

## 节点 B3：Query 理解 / 改写（V2）

- **技术**：一次 LLM 调用（当前为 deepseek-v4-flash），要求只输出 JSON；逐字段校验。
- **作用**：把口语化提问改写成检索友好的查询（vector_query），产出关键词列表（keyword_query），并推断过滤条件（filters，如"旧版"→ status=archive）。
- **目标**：用户怎么说是用户的事，系统按"最好检索的样子"去找。
- **衔接**：输出交给向量化（用改写后的查询）和过滤（用推断的条件）。
- **注意**：过滤推断必须保守——实测语义猜"领域"会与个人目录分类错位，猜错会把正确答案挡在门外；因此只保留高精度推断（归档意图、点名文件），且检索为 0 时自动放宽条件重试（渐进式放宽）。

## 节点 B4：Query Embedding（向量化）

- **技术**：与入库同一个 qwen3-embedding:4b 模型。
- **作用**：把（改写后的）问题转成 2560 维指纹。
- **衔接**：问题指纹是向量检索的输入；关键原则——文档和问题必须同源，否则相似度无意义。

## 节点 B5：Metadata Filter（元数据过滤）

- **技术**：Qdrant 的 Payload 过滤 + 字段索引。
- **作用**：检索前先圈范围。默认强制 status=active（归档与历史版本不参与回答）；用户可手动限定领域，Query 理解也可推断条件。
- **衔接**：上游是问题向量与过滤条件，下游是检索。
- **注意**：过滤是双刃剑——条件猜错会把正确答案挡在门外，所以配套了「渐进式放宽」容错（检索为 0 时自动去掉推断条件重试）。

## 节点 B6：数据库检索（向量通道）

- **技术**：Qdrant 的 COSINE 相似度 + HNSW 索引。
- **作用**：拿问题指纹找出过滤范围内最相近的 Top K 张卡片，每条带相似度分数。
- **目标**：RAG 的"R"——先检索。相似度分数同时是判断召回质量的第一依据。

## 节点 B7：BM25 关键词检索（V2）

- **技术**：jieba 中文分词 + rank-bm25 经典算法；查询时对过滤范围内的卡片现建内存索引。
- **作用**：关键词通道——按"词是否命中、是否稀有"打分，擅长专有名词、编号、缩写（如 YYYYMMDD、21/3a7），正好补向量检索"只懂意思、不懂精确"的盲区。
- **衔接**：与向量通道并行执行，结果交给融合节点。
- **注意**：BM25 分数与向量相似度分数完全不可比，融合只能用排名。

## 节点 B8：混合检索融合 RRF（V2）

- **技术**：Reciprocal Rank Fusion——每张卡片融合分 = Σ 1/(60 + 该通道内名次)。
- **作用**：把两路排名合并成一份；两路都命中的卡片天然排前；每条结果标注来源通道。
- **衔接**：上游是两路检索，下游是精排。
- **注意**：用排名不用原始分数，绕开两套分数量纲不一致的问题。

## 节点 B9：召回 Chunk

- **作用**：融合后的 Top K 候选（含通道标注、分数、档案），就是"查到的资料"。
- **衔接**：交给精排节点。

## 节点 B10：Rerank 精排（V2）

- **技术**：LLM 重排——召回先扩宽到 10 条候选，大模型逐条阅读内容，按相关度打 0~10 分（10~8 直接回答；7~4 主题相关；3~0 无关），按分精选 Top 5。可整体替换为 bge-reranker、Cohere Rerank 等专用重排模型。
- **目标**：粗排（向量/BM25）只看文字和指纹，精排才真正"理解内容"——这是排序质量的最后一道关。
- **衔接**：上游是融合候选，下游是资料组装。
- **注意**：候选编号与打分必须严格一一对应（实测踩过"编号错位一位导致高分行沉底"的坑）；打分失败整体回退粗排顺序。

## 节点 B11：合并 / 去重 + 多样性 + Top K 精选

- **技术**：忽略空白差异的全文判重；按 document_id 计数限制。
- **作用**：这里才是**真正意义上的 Top K 选择发生地**。K 指的是知识卡片（Chunk）的数量，默认 K=5，即最终选出 5 张卡片的原文发给大模型。
- **具体怎么截取的**：按 Rerank 精排后的分数从高到低，逐条遍历候选卡片，每一条依次做三个检查：
  1. **去重检查**：这条卡片的内容和已选中的某条完全相同（忽略空格换行差异）？→ 跳过，只保留分数更高的那条
  2. **多样性检查**：这张卡片所属的文档已经选了 3 条（CONTEXT_MAX_PER_DOC=3）？→ 跳过，把名额让给其他文档的卡片
  3. **Top K 截断**：已选满 K=5 条？→ 停止遍历，剩下的候选全部丢弃（丢弃原因记录在调试时间线"被丢弃的卡片"里）
- **为什么在这里选而不是在检索时选**：检索阶段故意宽召回 10 条（recall_k），给 Rerank 足够的候选池排序；Rerank 排完后，才在这里按 Top K 精选——宽进严出，保证排序质量。
- **目标**：给大模型一份不重复、不多不少、来源多元的资料。
- **注意**：列举类提问（"有哪些""列出全部"）会自动放大 K（5→10）并放开单文档上限（3→不限），由 token 上限做最终截断。

## 节点 B12：相邻补全（V3）

- **技术**：同文档、卡片序号连续的已选卡片自动拼接，相邻卡片间的 Overlap 重复部分只保留一份。
- **作用**：恢复被切片切断的完整语义——比如"关键结论"和它的下一段拼回一起读。
- **衔接**：发生在资料组装之前，拼好的内容作为一条资料进入附页。

## 节点 B13：上下文组装（Context）

- **技术**：模板化拼接 + 字数预算控制（上限 6000 字）。
- **作用**：把精选卡片整理成"考卷附页"：相关度降序编号 [1][2]…，每条标注来源/领域/章节/页码，总量不超限。
- **衔接**：输出就是 Prompt 里的"知识库资料"部分。
- **注意**：编号是引用来源的基础——大模型答案里的 [1] 就对应这里的编号。

## 节点 B14：LLM 调用（Prompt 组装 + 生成回答）

- **技术**：系统提示词（角色 + 规矩）+ 用户消息（资料附页 + 问题）组装成消息列表，通过 OpenAI 兼容接口调用云端大模型（当前 deepseek-v4-flash，可换任何兼容服务商或本地 Ollama）；流式输出（逐字返回）。
- **作用**：给大模型立规矩——只准按资料答、不知就承认、必须标引用编号、资料里的指令一律忽略（防提示词注入）、跟随用户语言。然后 RAG 的"G"——基于资料组织出人类可读的回答，并在句尾标注 [1] 这样的来源编号。
- **衔接**：上游是组装好的资料，下游是后处理与展示。
- **注意**：提示词是 RAG 可信度的根——本项目拒答率 100% 靠的就是"资料不足明确说不知道"这条规矩。API 异常必须翻译成人话（Key 无效/连不上/模型名不对/限流）；流式为空时自动回退重试一次。

## 节点 B15：后处理

- **作用**：提取答案文本、组装参考来源列表（编号 ↔ 卡片一一对应）、统计各环节耗时。
- **目标**：把"大模型的输出"变成"可核对、可追溯的交付物"——答案 + 来源卡片 + 调试时间线。

## 节点 B16：展示给用户

- **作用**：聊天气泡（用户右、回答左）+ 可展开的参考来源卡片（每条含档案标签与原文摘录）+ 一键复制 + 调试时间线。
- **目标**：不仅给答案，还给"凭什么信"的全部证据。

## 大模型被调用了几次？

### 入库流水线（上传/修改文件时触发，每个文件最多 1 次）

| 时机 | 调用内容 | 触发条件 |
|---|---|---|
| 解析后、切片前 | **LLM 自动打标签（V3.3）**：读取文件前 800 字，自动补全 topic（主题）和 tags（标签） | 仅当 Front Matter 没写、目录推断也没给出 topic 时才触发；已有值不覆盖 |

- 这是**一次性成本**：文件入库后不再调用，除非文件内容变化后重新入库；
- 可通过 `.env` 中 `AUTO_TAG=false` 关闭；
- 如果 Front Matter 已声明或目录推断已给出 topic/tags，则**跳过此调用**（零成本）。

### 问答流水线（每次提问都触发，V2 全开 = 3 次）

| 时机 | 调用内容 | 典型耗时 |
|---|---|---|
| ① 提问后、检索前 | Query 理解/改写：把口语化提问改写成检索查询，并推断过滤条件 | 1~5 秒 |
| ② 检索后、组装前 | Rerank 精排：逐条阅读 10 条候选卡片，按相关度打 0~10 分 | 2~4 秒 |
| ③ 资料组装后 | 回答生成：基于精选资料流式输出答案（唯一一次"说话"） | 1~3 秒 |

- 三个对应节点都可在「设置 → 检索设置」独立关闭：全关 = 只调 1 次（V1 模式，约 2 秒）；
- 混合检索（BM25）不调用大模型——纯本机计算，所以它是"性价比最高"的增强；
- 每次调用的输入输出规模都记录在调试时间线的对应节点里（Query 理解、Rerank、LLM 生成）。

### 总调用次数公式

```text
总调用 = (需要打标签的文件数 × 1) + (提问次数 × 3)
```

| 场景 | 入库调用 | 问答调用 | 总计 |
|---|---|---|---|
| 上传 1 个文件 + 问 1 个问题 | 0~1 次 | 3 次 | 3~4 次 |
| 批量导入 10 个文件 + 问 1 个问题 | 0~10 次 | 3 次 | 3~13 次 |
| 已入库 + 问 10 个问题 | 0 次 | 30 次 | 30 次 |
| 同一问题重复问（缓存命中） | 0 次 | **0 次** | 0 次 |

---

## 附：三个贯穿全程的设计原则

1. **档案（Metadata）是一等公民**：入库时生成、检索时过滤、回答时引用、管理台里可改——一条链路贯穿始终。
2. **宁可不增强，不能中断**：Query 理解失败回退原始问题、打分失败回退粗排、过滤筛空自动放宽——每个增强节点都有回退路径。
3. **一切可见**：每个节点的输入输出都可以单独查看（各模块可独立运行），调试时间线把"为什么召回这条知识"完整呈现——这是学习 RAG 最大的捷径。
