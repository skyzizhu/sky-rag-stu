# 生成与引用编排（Generation）

对应需求：FR-GEN-001 ~ 004。完整规则与验收见「需求文档 → RAG 编排需求与验收」。

生成编排解决"答案可信、可溯源、不编造"：相邻补全 → 上下文组装（token 预算）→ LLM 流式生成 → 引用对位。核心设计约束：

1. **引用对位**：上下文编号、LLM 引用、来源列表三者同源
2. **拒答优先**：证据不足时明确说明资料不足，不编造
3. **流式体验**：生成过程流式输出，失败/半截答案不入缓存
4. **安全渲染**：卡片原文渲染前转义（网页内容不可信）
